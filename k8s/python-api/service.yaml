apiVersion: v1
kind: Service
metadata:
  name: python-api-service
  namespace: default
spec:
  selector:
    app: python-api
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
